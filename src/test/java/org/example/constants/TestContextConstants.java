package org.example.constants;
/**
 * This class defines constants used as keys for storing and retrieving objects
 * in the test context. The test context is often a shared storage mechanism
 * (e.g., a `Map`) used to pass data between different parts of the test framework.
 */
public class TestContextConstants {
    public static final String DRIVER = "driver";
    public static final String REPORTER = "reporter";
}
